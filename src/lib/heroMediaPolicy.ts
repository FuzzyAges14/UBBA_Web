/**
 * Hero video is disabled until the academy supplies owned dojang footage.
 * The prior WebsiteDojo / Vimeo stock loop must not ship on this site.
 */
export function shouldLoadHeroVideo(): boolean {
  return false
}
